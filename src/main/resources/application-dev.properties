server.port=8090
spring.datasource.url=jdbc:postgresql://localhost:5432/paymentsystem
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.database=POSTGRESQL
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto = update
spring.flyway.enabled=false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
senderemail=${MAIL_SENDER_EMAIL}
spring.mail.username=${MAIL_SENDER_USER}
spring.mail.password=${MAIL_SENDER_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

jwt.secret=${JWT_SECRET:senha-local-quando-nao-encontra-a-variavel-de-ambiente}